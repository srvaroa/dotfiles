# ~/.bash_profile: executed by bash for login shells
if [ -f ~/.bash_profile ]; then
    source ~/.bash_profile
fi

if [ -f ~/.aliases ]; then
    source ~/.aliases
fi

if [ -f ~/.env ]; then
    source ~/.env
fi
